import pygame

#Simple player object
'''class Bot(pygame.sprite.Sprite):

    # Initialization
    def __init__(self,x,y,image,speed):
        pygame.sprite.Sprite.__init__(self)
        self.x = x
        self.y = y
        self.image = image
        self.in_node = False
        self.next_node = 0
        self.directionx = -x
        self.directiony = -y
        self.speed = speed
        #self.angle = angle
        #self.speed = speed

    #Method to draw object
    def draw(self):
        window.blit(self.image,(self.x,self.y))#mudar para load image algo

    #Method to move object (special input of dx and dy)
    def follow(self):
        if self.in_node:
            #self.next_node = Djikstra algorithm

        #Calculating new direction
        self.directionx = next_node[0]-x #Fix next_node data type so it has x and y
        self.directiony = next_node[1]-y

        #Updating course
        self.x += self.speed*self.directionx/(self.directionx**2+self.directiony**2)**0.5
        self.y += self.speed*self.directiony/(self.directionx**2+self.directiony**2)**0.5'''
        

